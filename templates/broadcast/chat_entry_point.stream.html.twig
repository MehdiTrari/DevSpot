<turbo-stream action="replace" target="chat-entry-point">
    <template>{{ include('_chat_entry_point.html.twig', {user: user, unreadCount: unreadCount}, with_context = false) }}</template>
</turbo-stream>
